//! `rprof run` implementation. Placeholder — filled in after schema is in place.

use anyhow::Result;

use crate::cli::RunArgs;

pub fn run(_args: RunArgs) -> Result<u8> {
    anyhow::bail!("`rprof run` is not yet implemented")
}
