//! `rprof view` implementation. Placeholder — filled in after capture works.

use anyhow::Result;

use crate::cli::ViewArgs;

pub fn run(_args: ViewArgs) -> Result<u8> {
    anyhow::bail!("`rprof view` is not yet implemented")
}
